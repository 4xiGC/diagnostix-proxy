# DiagnostiX RVP
#
# node_modules/ only. package-lock.json is NOT listed: none of the four
# DiagnostiX repos ignores it, and none tracks it.

node_modules/

# Before-images of subscribers rows carry email addresses. They live in
# C:/Users/SIMON/diagnostix-private/before-images, never in a repo.
*before-image*
